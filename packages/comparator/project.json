{
  "root": "packages/comparator",
  "sourceRoot": "packages/comparator",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "packages/comparator",
        "outputPath": "dist/packages/comparator"
      },
      "configurations": {
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "options": {
        "buildTarget": "comparator:build",
        "dev": true
      },
      "configurations": {
        "production": {
          "buildTarget": "comparator:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "comparator:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/packages/comparator"],
      "options": {
        "jestConfig": "packages/comparator/jest.config.js",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/comparator/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
