export enum PRIORITY {
  READ,
  COMPUTE,
  WRITE,
}

export enum URGENCY {
  CURRENT_FRAME,
  NEXT_FRAME,
}

export enum PHASE {
  INTERACTING,
  RENDERING,
}

export const PRIORITIES_IN_ORDER = [
  PRIORITY.READ,
  PRIORITY.COMPUTE,
  PRIORITY.WRITE,
]
