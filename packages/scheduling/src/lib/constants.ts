export enum PRIORITY {
  BEFORE_RENDER = 'BEFORE_RENDER',
  READ = 'READ',
  COMPUTE = 'COMPUTE',
  WRITE = 'WRITE',
}

export const IDLE_PRIORITIES_IN_ORDER = [PRIORITY.BEFORE_RENDER]

export const RENDER_PRIORITIES_IN_ORDER = [
  PRIORITY.READ,
  PRIORITY.COMPUTE,
  PRIORITY.WRITE,
]

export const PRIORITIES_IN_ORDER = [
  ...IDLE_PRIORITIES_IN_ORDER,
  ...RENDER_PRIORITIES_IN_ORDER,
]
